str.count #retorna a qtde de conjuntos presentes na string

str.isalpha #retorna False se a string contiver algum caracter que n seja letras

str.isdigit #retorna False se a string contiver algum caracter que nao seja numero.

str.replace () #substitui uma porção da string por outro conteúdo.

str.title() #retorna a string apitaliz(inciais em maiúsculo)

str.join() #junta cada item da string com um delimitador especificado. Inverso de split.

str.strip() #Retorna a string sem caracteres iniciais ou finais que estejam na string: str.strip('ab') retira os caracteres da string.
