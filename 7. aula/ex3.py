pesos = []
alturas = []

for pessoa in range (1, 6):
    peso = peso = float(input(f"Digite o peso da pessoa {pessoa}: "))
    altura= float(input(f"Digite a altura da pessoa {pessoa} : "))

pesos.append(peso)
alturas.append(altura)

imc = peso / (altura ** 2)
imcs.append(imc)

maior_imc = max(imc)
menor_imc = min(imc)

print(f"\nResultados do IMC: ")
for pessoa imc in enumerate(imcs,)

